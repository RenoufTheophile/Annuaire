@model Annuaire.Models.Personne
@{
    ViewBag.Title = "Ramdom";
}








<h1>Create</h1>

<h4>Personne</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Ramdom">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for=@Model.root.results[0].name.last id="last" class="control-label"></label>
                <input asp-for=@Model.root.results[0].name.last text="test" id="last" class="form-control" />
                <span asp-validation-for=@Model.root.results[0].name.last id="last" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for=@Model.root.results[0].name.first id="first" class="control-label"></label>
                <input asp-for=@Model.root.results[0].name.first id="first" class="form-control" />
                <span asp-validation-for=@Model.root.results[0].name.first id="first" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for=@Model.root.results[0].registered.date id="date" class="control-label"></label>
                <input asp-for=@Model.root.results[0].registered.date id="date" class="form-control" />
                <span asp-validation-for=@Model.root.results[0].registered.date id="date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for=@Model.root.results[0].phone id="phone" class="control-label"></label>
                <input asp-for=@Model.root.results[0].phone id="phone" class="form-control" />
                <span asp-validation-for=@Model.root.results[0].phone id="phone" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for=@Model.root.results[0].registered.service id="service" class="control-label"></label>
                <input asp-for=@Model.root.results[0].registered.service id="service" class="form-control" />
                <span asp-validation-for=@Model.root.results[0].registered.service id="service" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
 
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}


@model Annuaire.Models.Root


@{ ViewData["Title"] = "Ramdom User"; }

<h1>Create</h1>

<h4>Personne</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Ramdom">
            @for (int i=0; i<Model.results.Count; i++)
                    { 

            <div class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.results[i].name.first" class="control-label"></label>
                <input asp-for="@Model.results[i].name.first" class="form-control" />
                <span asp-validation-for="@Model.results[i].name.first" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.results[i].name.last" class="control-label"></label>
                <input asp-for="@Model.results[i].name.last" class="form-control" />
                <span asp-validation-for="@Model.results[i].name.last" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.results[i].registered.date" class="control-label"></label>
                <input asp-for="@Model.results[i].registered.date" class="form-control" />
                <span asp-validation-for="@Model.results[i].registered.date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.results[i].phone" class="control-label"></label>
                <input asp-for="@Model.results[i].phone" class="form-control" />
                <span asp-validation-for="@Model.results[i].phone" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.results[i].registered.service " class="control-label"></label>
                <input asp-for="@Model.results[i].registered.service " class="form-control" />
                <span asp-validation-for="@Model.results[i].registered.service " class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
            }
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
